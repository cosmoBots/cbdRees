#ifndef _PRJ_TYPES
#define _PRJ_TYPES

#include <Arduino.h>

#define TRUE true
#define FALSE false

#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) < (y) ? (x) : (y))

typedef uint16_t t_tbd;

#endif /* _PRJ_TYPES */
