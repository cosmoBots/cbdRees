#ifndef _{$replace}DEBOUNCE_DRE_DIAG_H
#define _{$replace}DEBOUNCE_DRE_DIAG_H
    /// Input
    bool enable_{$replace}Raw;
    uint16_t {$replace}Raw;
    /// Output
    bool enable_{$replace}Filt;
    uint16_t {$replace}Filt;
#endif /* _{$replace}DEBOUNCE_DRE_DIAG_H */
