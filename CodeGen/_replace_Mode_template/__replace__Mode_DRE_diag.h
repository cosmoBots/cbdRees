#ifndef _{$replace}MODE_DRE_DIAG_H
#define _{$replace}MODE_DRE_DIAG_H
    /// Input
    bool enable_{$replace}Sense;
    uint16_t {$replace}Sense;
    /// Output
    bool enable_{$replace}Mode;
    t_enum_{$replace}Mode {$replace}Mode;
#endif /* _{$replace}MODE_DRE_DIAG_H */
